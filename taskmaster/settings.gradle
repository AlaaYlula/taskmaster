pluginManagement {
    repositories {
        gradlePluginPortal()
        google()
        mavenCentral()
    }
}
dependencyResolutionManagement {
    //repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS) // do this for location
    repositories {
        google()
        mavenCentral()
    }
}
rootProject.name = "taskMaster"
include ':app'
